apiVersion: v1
kind: Pod
metadata:
  name: pod-resources-test
  namespace: policy
  annotations:
    smaitic.com/inject-secrets-poc: "pod-resources-test"
spec:
  containers:
    - name: app
      image: busybox:latest
      command: ["java", "-jar", "app.jar"]
