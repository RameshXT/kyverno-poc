apiVersion: v1
kind: Pod
metadata:
  name: demo-03-plain-unannotated
  namespace: {{ .Release.Namespace }}
spec:
  containers:
    - name: app
      image: {{ .Values.app.image }}
      command: ["sleep", "3600"]
